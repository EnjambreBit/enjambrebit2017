<div class="ui large container ebit-content">
  {{#if media.isDesktop}}
    <div class="ebit-section-bg portfolio"></div>
  {{/if}}
  <div class="ui secondary pointing fluid menu">
    <h1 class="ui huge header">
      <i class="red folder open icon"></i>
      <div class="content">
        Trabajos
      </div>
    </h1>
  </div>
  {{ebit-galeria}}
  {{outlet}}
</div>
